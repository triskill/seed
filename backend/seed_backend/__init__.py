"""Seed v0.1 backend package.

Houses the FastAPI orchestrator that drives the pi agent loop and manages
the Flask webapp subprocess. Empty in Task 0.1; real code lands in
Task 0.2+ (`service.py`, `config.py`, `flask_manager.py`, etc.).
"""
__version__ = "0.0.1"
