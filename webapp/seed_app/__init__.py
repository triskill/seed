"""Seed v0.1 webapp package.

Flask + SQLite app that the worker agent mutates. The Android App screen
renders this inside a WebView at `http://127.0.0.1:7778/`.

Task 0.1: minimal skeleton with `/` and `/api/ping`. Real routes
(todos, habit tracker, ...) are added by the worker in later phases.
"""
__version__ = "0.0.1"
