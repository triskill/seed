"""Seed v0.1 webapp workspace.

Marker package so pytest can resolve cross-package test imports when
invoked as `pytest backend/ webapp/` from the worktree root. The actual
Flask app lives in the installable `seed_app` subpackage.
"""
